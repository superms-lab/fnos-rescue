# 第六阶段：发布前安全加固与质量验收

状态：内容完成；发布动作未执行。

## 完成内容

- 前端依赖由 `latest` 改为精确版本，并继续由 lockfile 固定完整依赖树。
- npm 安装默认禁止依赖安装脚本，CI 使用 `npm ci` 并拒绝高危漏洞。
- 增加仓库敏感信息扫描，覆盖私钥、GitHub Token、AWS Key、验证机地址、用户名和本机私人路径。
- 增加 CycloneDX 1.5 SBOM 生成器，列出 Web 依赖及精确版本。
- 为所有发行文件生成 SHA-256 `SHA256SUMS`。
- CI 增加独立 Web 构建、安全审计和敏感信息门禁。
- 增加一键 release preflight，检查版本一致性、Git 差异、敏感信息、Live profile、全量测试、前端生产构建、依赖审计、fnOS 包和发行元数据。

## 当前验证结果

- Python 自动化测试：53 项通过。
- npm audit：0 个已知漏洞。
- 前端生产构建：通过。
- 敏感信息扫描：通过。
- Live profile 校验：通过。
- SBOM 与 SHA-256 清单：已成功生成。

## 仍属于发布动作的事项

以下事项没有得到用户授权，因此本阶段不执行：提交当前工作区、创建或切换远端仓库、推送、创建 `v0.1.0` Tag、GitHub prerelease。第五阶段的 ISO 还需要 Linux CI 实际构建和虚拟机启动证据，不能在 macOS 本机伪造完成状态。
